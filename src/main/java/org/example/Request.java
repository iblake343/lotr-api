package org.example;

public class Request {
    // Cache every character -> both files. || array
    // Cache every quote -> both files. || array
    // Return data as json.
    // Return Specific Characters.
    // Return Specific Quotes.
}
